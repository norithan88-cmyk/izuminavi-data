# いずみなび イベント自動更新

安城市公式イベントカレンダーの当月・翌月を毎日取得し、終了したイベントを除外して `events.json` を更新します。

## 更新時刻

GitHub Actionsで毎日6時15分ごろ（日本時間）に更新します。管理画面から手動実行もできます。

## 安全策

- 公式ページから1件も取得できなかった場合は失敗として終了し、既存データを上書きしません。
- 開催日が過ぎたイベントは次回更新時にJSONから外れます。
- 表示側は取得に失敗しても、TOPページの案内表示を残します。
